# Project Rules & Custom Instructions

## 🗑️ سياسة التعامل مع الملفات المكررة (Duplicate Files Policy)

إذا اكتشفت أثناء تنفيذ المهام ملفاً يبدو مكرراً (يعمل نفس عمل ملف آخر موجود)، اتبع هذا الإجراء بدقة وبدون استثناء:

---

### الخطوة الأولى: لا تحذف مباشرة
ممنوع تماماً حذف أي ملف مكرر بشكل فوري. حتى لو بدا واضحاً أنه مكرر، قد يكون مستخدماً في مكان لا تراه.

---

### الخطوة الثانية: ابحث عن كل الاستخدامات
ابحث في كل المشروع عن اسم الملف المشتبه في تكراره. ابحث عن:
1. **اسم الفئة الرئيسية** داخل الملف (مثلاً: `BookingUtils`).
2. **اسم الملف نفسه** في الاستيرادات (`import`).
3. **اسم الدالة أو الدوال الرئيسية** داخل الملف.
4. **اسم النموذج أو الفئة** إذا كان الملف يحتوي على `data class` أو `enum` أو `interface`.

استخدم خاصية "البحث في كل المشروع" (Find in Files).

---

### الخطوة الثالثة: قارن بين الملفين
ضع الملفين المشتبه فيهما جنباً إلى جنب وقارن:
- **قارن الفئات:** هل الفئات لها نفس الاسم؟ نفس الحقول؟ نفس الدوال؟
- **قارن المنطق:** هل الكود في كليهما يقوم بنفس الشيء؟ هل هناك اختلافات جوهرية؟
- **قارن الاستخدامات:** هل كل ملف مستخدم في أماكن مختلفة؟ أم أن أحدهما مستخدم والآخر لا؟
- **قارن التواريخ:** أي ملف أحدث؟

اكتب تقريراً بهذه المقارنة قبل اتخاذ أي قرار.

---

### الخطوة الرابعة: حدد أي الملفين يمكن حذفه
الملف الذي يمكن حذفه هو الذي يحقق الشروط التالية مجتمعة:
- **الشرط الأول:** لا يوجد أي `import` يشير إليه في أي ملف آخر بالمشروع.
- **الشرط الثاني:** الفئة الرئيسية داخله لا تُستدعى في أي مكان.
- **الشرط الثالث:** كل الدوال أو الفئات داخله موجودة بشكل مكافئ في الملف الآخر الذي سيبقى.
- **الشرط الرابع:** الملف الآخر (الذي سيبقى) أكثر اكتمالاً أو أحدث.
- **الشرط الخامس:** لا يوجد أي تعليق في المشروع يشير إلى أن هذا الملف ضروري.

إذا لم تتحقق كل الشروط الخمسة، لا تحذف الملف. أبلغ عنه فقط.

---

### الخطوة الخامسة: إذا تحققت الشروط، اسأل قبل الحذف
لا تحذف من تلقاء نفسك. أرسل استفساراً واضحاً بالصيغة التالية:

> "يوجد ملفان يبدو أنهما مكرران:
> · الملف الأول: [مسار الملف]
> · الملف الثاني: [مسار الملف]
> 
> نتيجة البحث:
> · عدد الاستخدامات للملف الأول: [رقم]
> · عدد الاستخدامات للملف الثاني: [رقم]
> · الملف المستخدم فعلياً: [اسم الملف]
> · الملف غير المستخدم: [اسم الملف]
> 
> تأكيدي أن حذف [اسم الملف غير المستخدم] لن يؤثر على التطبيق:
> · لا يوجد import يشير إليه في أي مكان
> · لا توجد أي دالة داخله تُستدعى
> · كل وظائفه موجودة في الملف الآخر
> · الملف الآخر أحدث وأكمل
> 
> السؤال: هل تسمح بحذف الملف غير المستخدم؟"

---

### الخطوة السادسة: انتظر الإجابة قبل الحذف
لا تحذف أي ملف حتى يُطلب منك الحذف صراحة.

---

### 📋 قائمة الملفات المرجعية المشتبه بها
- **ملفات دفع / مزامنة:** `SyncManager.kt` في الأدوات والمزامنة.
- **ملفات مساعدة:** `Helper.kt` و `Helpers.kt`.
- **ملفات إشعارات:** `NotificationChannels.kt` و `NotificationHelper.kt` و `ChatNotificationHelper.kt`.
- **ملفات نماذج:** `Category.kt` و `CategoryEntity.kt`.
- **مكتبات خارجية (ممنوع الحذف نهائياً):** `leaflet.js`, `leaflet.css`, `leaflet.markercluster.js`, `MarkerCluster.css`, `MarkerCluster.Default.css`.

---

### 🚨 القاعدة الذهبية
عند الشك، لا تحذف. أبلغ فقط. الإبلاغ أفضل من الحذف دائماً.

---

## 📌 توثيق البنية الحالية والملفات الخاصة

### 1️⃣ تحذير بشأن `SyncManager`:
يوجد ملفان بنفس الاسم في المشروع:
- `utils/SyncManager.kt` = مزامنة محلية (Room / SQLite Cache)
- `sync/SyncManager.kt` = مزامنة شبكية (Firestore Remote Sync)
هما ليسا مكررين بل مكملان لبعضهما. لا تحذف أياً منهما. الاسم المشابه كان نتيجة التسمية الأولية للتصميم الوظيفي.

### 2️⃣ تحذير بشأن مكتبات الخرائط:
الملفات التالية في مجلد `assets/` هي مكتبات خارجية (Leaflet و MarkerCluster):
- `leaflet.js`, `leaflet.css`
- `leaflet.markercluster.js`
- `MarkerCluster.css`, `MarkerCluster.Default.css`
لا تحذفها مطلقاً. يستخدمها ملف `map.html` وتُكسر الخريطة التفاعلية بالكامل لو حُذفت.

### 3️⃣ قسم ملفات الإشعارات (Notification Files):
المشروع يحتوي على ثلاثة ملفات إشعارات، كل واحد له دور واضح ومختلف:

- **الملف الأول: `utils/NotificationChannels.kt`**
  · الدور: إنشاء قنوات الإشعارات العامة الأساسية
  · الحالة: مؤقت، سيُدمج لاحقاً
  · لا تُعدّله الآن

- **الملف الثاني: `utils/NotificationHelper.kt`**
  · الدور: إشعارات الإدارة الحرجة والعادية
  · الحالة: حرج، يعتمد عليه FCMService.kt
  · ممنوع حذفه أو تعديله

- **الملف الثالث: `utils/ChatNotificationHelper.kt`**
  · الدور: إشعارات المحادثات والوسائط والطلبات العاجلة مع ميزات متقدمة
  · الحالة: حرج، يعتمد عليه FCMService.kt و NotificationScheduler.kt
  · ممنوع حذفه أو تعديله

قاعدة عامة: لا يُحذف أي ملف إشعارات إلا بعد نقل كل قنواته إلى ملف آخر والتأكد من أن MyApplication.kt يستدعي الملف الباقي فقط.

### 4️⃣ قسم توحيد الأنظمة المتوازية (المرحلة الثالثة):

- **القسم الأول — توحيد الأدوار:**
  ملف `UserRole.kt` هو المصدر الموحّد لكل الأدوار. يجب استخدامه في الكود الجديد بدلاً من النصوص. القيم النصية القديمة موجودة للتوافقية فقط.

- **القسم الثاني — توحيد حالات الحجز:**
  ملف `BookingStatusColors.kt` هو المصدر الموحّد لألوان ونصوص حالات الحجز. القيمة `ACCEPTED` هي المعتمدة، و `APPROVED` قديمة للتوافقية.

### 5️⃣ الديون التقنية المعروفة (Technical Debts):

- **قسم OrdersScreenLayout:**
  ملف `OrdersScreenLayout.kt` يحتوي على 500+ سطر في Composable واحد. الحالة: يعمل بشكل مستقر. القاعدة: ممنوع إضافة ميزات جديدة كبيرة إليه حتى يُقسَّم. الخطة المستقبلية: يُقسَّم إلى 5 ملفات (`OrdersScreenLayout`, `InstantRequestOrderCard`, `StoreOrderCard`, `OrderDeletionDialog`, `DeleteAllOrdersDialog`) قبل الإطلاق الرسمي.

- **قسم 4 شاشات القوائم:**
  شاشات `StoresScreen` و `RestaurantsScreen` و `MedicalCentersScreen` و `PropertiesScreen` تحتوي على منطق متشابه. الحالة: مستقرة. الخطة: تُوحَّد في ملف واحد بعد تقسيم `OrdersScreenLayout`.

- **قسم 3 نماذج التقييم:**
  يوجد ثلاثة نماذج تقييم في المشروع:
  · `MultiDimensionRatingDialog` (الأفضل والأشمل)
  · `ReviewInput` (مبسط)
  · `EnhancedReviewInput` (في `ProfileReviewCard.kt` مع صور)
  الحالة: مستقرة. الخطة: يُبقى على `MultiDimensionRatingDialog` ويُدمج الآخران.

- **قسم AdminPanelLayout:**
  ملف `AdminPanelLayout.kt` يحتوي على 9140 سطر. الحالة: يعمل. الخطة: يحتاج إلى تقسيم إلى 20+ ملف فرعي في مرحلة لاحقة.

- **قسم ملفات الإشعارات الثلاثة:**
  يوجد ثلاثة ملفات إشعارات:
  · `NotificationChannels.kt` (قنوات عامة)
  · `NotificationHelper.kt` (إشعارات الإدارة)
  · `ChatNotificationHelper.kt` (إشعارات المحادثات والطوارئ)
  الحالة: مستقرة وكلها مستخدمة. الخطة: دمجها في ملف واحد لاحقاً.

- **قسم ملفي SyncManager:**
  يوجد ملفان بنفس الاسم:
  · `utils/SyncManager.kt` (مزامنة محلية)
  · `sync/SyncManager.kt` (مزامنة شبكية)
  هما ليسا مكررين بل مكملان. ممنوع حذف أحدهما.

- **قسم مكتبات الخرائط:**
  ملفات `leaflet.js`, `leaflet.css`, `leaflet.markercluster.js`, `MarkerCluster.css`, `MarkerCluster.Default.css` في مجلد `assets/` هي مكتبات خارجية يستخدمها `map.html`. ممنوع حذفها.

### 6️⃣ الحقول المهجورة في النماذج (Deprecated Fields):

- **قسم BookingEntity:**
  *الحقول المعتمدة:*
  · `customerName` (الاسم)
  · `customerPhone` (الهاتف)
  · `customerArea` (المنطقة)
  · `date` (التاريخ)
  · `time` (الوقت)
  
  *الحقول المهجورة (للتوافقية فقط):*
  · `fullName`, `clientName`, `userName`
  · `clientPhone`, `userPhone`
  · `fullAddress`, `clientAddress`
  · `dateString`, `timeString`

- **قسم ChatChannel:**
  *الحقول المعتمدة:*
  · `title`, `participantNames` (العنوان)
  · `targetPhone` (الهاتف)
  · `groupAvatarUrl`, `participantPhotos` (الصورة)
  
  *الحقول المهجورة:*
  · `providerName`, `clientName`, `targetName`, `userName`, `customerName`
  · `customerPhone`
  · `providerId`, `clientId`


### 7️⃣ الحمايات الزمنية لحالات التحميل (Load State Timeouts):

تمت إضافة حمايات زمنية (Timeouts) لحالات التحميل في:
- `BookingCalendarScreen` — 30 ثانية كحد أقصى لحالة الإرسال (`isSubmitting`) لتجنب بقاء الزر معطلاً للأبد في حال حدوث أي خطأ غير متوقع.
- `StatusViewModel.refreshData` — إعادة تشغيل وتحديث مراجع مستمعي قاعدة البيانات (Flow Stream collection) عند الضغط على زر "تحديث" لتجاوز أي تعليق أو تجمد للبيانات.
- `OrdersScreenLayout` — شاشة الطلبات لا تحتوي على حالة تحميل داخلية صريحة (تعتمد مباشرة على viewModel StateFlow)، ولذا تم الالتزام بالاستراتيجية الاحترازية بعدم التعديل العشوائي.

*القاعدة العامة:* أي حالة تحميل أو إرسال يجب أن يكون لها حد زمني لتجنب تعليق واجهات المستخدم.


### 8️⃣ قواعد تعديل الكود (Code Modification Rules):

قواعد صارمة لأي تعديل مستقبلي:
1. لا تعدّل `MyApplication.kt` أو `MainActivity.kt` دون مراجعة كاملة وتبرير هندسي واضح.
2. لا تحذف أي `try/catch` موجود في ملفات التهيئة الأساسية للمشروع.
3. لا تحذف أي حقل من أي نموذج بيانات قائم ومستخدم للتوافقية العكسية — أضف بدلاً من أن تحذف.
4. عند الشك أو عدم التيقن، أبلغ في التقرير ولا تقم بالحذف أو التعديل من تلقاء نفسك.
5. أي تعديل على ملفات التهيئة والربط للنظام يجب أن يخضع لاختبار وبناء فوري للتحقق من سلامة البناء (compile_applet).


### 9️⃣ نماذج التقييم الثلاثة

المشروع يحتوي على 3 نماذج تقييم، كل واحد له سياق مختلف:

**ReviewInput** (`ui/components/ReviewInput.kt`)
- الاستخدام: التقييم العام للخدمات (متاجر، مطاعم، عقارات، طبي)
- الميزات: نجوم إجمالية + تعليق
- الحالة: مستخدم بكثرة

**MultiDimensionRatingDialog** (`ui/dialogs/MultiDimensionRatingDialog.kt`)
- الاستخدام: التقييم المفصل للفنيين في الطلبات العاجلة
- الميزات: 4 أبعاد (جودة، سرعة، احترافية، سعر) + تعليق
- الحالة: مستخدم في شاشة واحدة

**EnhancedReviewInput** (`ui/screens/entities/ProfileReviewCard.kt`)
- الاستخدام: حالياً غير مستخدم
- الميزات: نجوم إجمالية + تعليق + صور
- الحالة: معرف للاستخدام المستقبلي

**قرار معماري:**
لا يتم توحيد النماذج الثلاثة لأنها تخدم سياقات مختلفة، والتوحيد سيضيف تعقيداً بدلاً من التبسيط.

**قاعدة:**
كل نموذج يبقى في مكانه. عند الحاجة لنموذج تقييم جديد، اختر الأنسب من الثلاثة بدلاً من إنشاء رابع.




